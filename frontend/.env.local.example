# Frontend Environment Variables

# Backend API base URL
# Note: Include /api/v1 in the URL as the frontend code appends specific endpoints (e.g., /todos)
NEXT_PUBLIC_API_URL=http://localhost:8001/api/v1
